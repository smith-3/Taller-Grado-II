\chapter{MARCO TEÓRICO}
\label{chap:marco-teorico}

Este capítulo establece los fundamentos teóricos y tecnológicos relacionados con la ingeniería de software móvil moderna, la arquitectura de software escalable, los algoritmos de optimización y la inteligencia artificial en el borde (Edge AI). Además, se abordan las técnicas de adquisición y procesamiento de datos desde fuentes heterogéneas, como páginas web y documentos estructurados.

El marco conceptual presentado proporciona la base necesaria para comprender las decisiones de diseño e implementación descritas en capítulos posteriores, fundamentando cada elección tecnológica en principios establecidos por la comunidad académica y la industria del desarrollo de software.

\section{Metodologías de Desarrollo de Software}
\label{sec:metodologias}

Las metodologías de desarrollo de software establecen el marco de trabajo para organizar, planificar y ejecutar proyectos de software. Se distinguen dos enfoques principales: las metodologías tradicionales (como Waterfall) y las metodologías ágiles.

\subsection{Metodologías Ágiles}
Las metodologías ágiles surgieron como respuesta a las limitaciones de los enfoques tradicionales en entornos de alta incertidumbre. El Manifiesto Ágil (2001) establece cuatro valores fundamentales: priorizar individuos sobre procesos, software funcional sobre documentación, colaboración con el cliente sobre contratos, y respuesta al cambio sobre seguir un plan \cite{agile_manifesto}.

\subsection{Kanban}
Kanban es un método de gestión visual del trabajo que limita el trabajo en progreso (WIP) para maximizar el flujo y la eficiencia. Originado en el sistema de producción de Toyota, se adaptó al desarrollo de software para visualizar el flujo de trabajo, identificar cuellos de botella y promover la mejora continua \cite{kanban_anderson}.

El Cuadro \ref{tab:metodologia_comparison} compara las principales metodologías ágiles utilizadas en desarrollo de software.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & Kanban & Scrum \\
\midrule
Iteraciones & Flujo continuo & Sprints fijos (2-4 semanas) \\
Roles Definidos & Flexibles & Product Owner, Scrum Master \\
Límite de Trabajo & WIP por columna & Capacidad del sprint \\
Cambios & En cualquier momento & Solo entre sprints \\
Reuniones & Opcionales & Ceremonias obligatorias \\
\bottomrule
\end{tabularx}
\caption{Comparación de metodologías ágiles.}\label{tab:metodologia_comparison}
\small{Fuente: Basado en Anderson (2010) \cite{kanban_anderson} y Schwaber (2004) \cite{scrum_schwaber}}
\end{table}

Kanban resulta especialmente adecuado para proyectos con un desarrollador único, donde la flexibilidad para cambiar prioridades y la ausencia de ceremonias formales maximizan la productividad.

\section{Ingeniería de Software para Dispositivos Móviles}
\label{sec:ingenieria-movil}

El desarrollo de aplicaciones móviles ha transitado desde enfoques imperativos hacia paradigmas declarativos y reactivos. Este cambio prioriza la eficiencia en el manejo de recursos limitados y asegura una interacción continua con el usuario.

\subsection{Arquitectura de Plataforma Android}
Android es un sistema operativo basado en el núcleo Linux, diseñado para dispositivos con pantalla táctil. Su arquitectura se divide en capas: el kernel de Linux, la capa de abstracción de hardware (HAL), el tiempo de ejecución de Android (ART) y el marco de trabajo de aplicaciones (Application Framework) \cite{android_arch}.

El desarrollo nativo, utilizando el Android SDK (Software Development Kit), permite un acceso directo a las APIs del sistema. En consecuencia, este enfoque proporciona un rendimiento optimizado en comparación con soluciones híbridas, aspecto relevante en tareas intensivas como la inferencia de modelos de IA \cite{android_programming}.

El desarrollo móvil nativo se realiza principalmente en dos plataformas dominantes: Android e iOS. El Cuadro \ref{tab:android_vs_ios} presenta una comparación de las características principales de ambas plataformas.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{4cm} p{4cm} p{4cm}}
\toprule
Característica & Android & iOS \\
\midrule
Sistema Operativo Base & Linux Kernel & Darwin (UNIX) \\
Lenguaje Principal & Kotlin/Java & Swift/Objective-C \\
IDE Oficial & Android Studio & Xcode \\
Cuota de Mercado Global & Aproximadamente 70\% & Aproximadamente 28\% \\
Código Fuente & Open Source (AOSP) & Propietario \\
\bottomrule
\end{tabularx}
\caption{Comparación entre plataformas Android e iOS.}\label{tab:android_vs_ios}
\small{Fuente: Basado en StatCounter (2024) \cite{statcounter_mobile} y documentación oficial \cite{android_arch}}
\end{table}

La elección de plataforma depende de factores como el mercado objetivo, los recursos disponibles y los requisitos técnicos específicos. Android presenta ventajas en términos de cuota de mercado global y flexibilidad del ecosistema abierto.

Kotlin es un lenguaje moderno y estáticamente tipado que interopera completamente con Java. El Cuadro \ref{tab:kotlin_vs_java} presenta una comparación entre estos lenguajes.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & Kotlin & Java \\
\midrule
Null Safety & Nativo (tipos nullables) & Opcional (anotaciones) \\
Concurrencia & Coroutines (ligeras) & Threads (pesados) \\
Código Boilerplate & Mínimo & Extenso \\
Data Classes & Nativo (data class) & Manual o Lombok \\
Soporte Google & Preferido desde 2019 & Histórico \\
\bottomrule
\end{tabularx}
\caption{Comparación entre Kotlin y Java para Android.}\label{tab:kotlin_vs_java}
\small{Fuente: Basado en Kotlin Documentation \cite{kotlin_docs} y Android Developers \cite{android_programming}}
\end{table}

Kotlin ofrece características como Coroutines y Flow para la gestión eficiente de la concurrencia asíncrona, reduciendo significativamente el código repetitivo en comparación con Java.

% SECCIÓN COMENTADA - Entorno de Desarrollo Integrado
% \\subsection{Entorno de Desarrollo Integrado para Android}
% 
% Un Entorno de Desarrollo Integrado (IDE) proporciona herramientas integradas para escribir, depurar y compilar código. Para el desarrollo de aplicaciones Android, existen diversas opciones disponibles en el mercado.
% 
% El Cuadro \\ref{tab:ide_comparison} presenta una comparación entre los principales IDEs utilizados en el desarrollo Android.
% 
% \\begin{table}[H]
% \\small
% \\centering
% \\begin{tabularx}{\\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
% \\toprule
% Característica \& Android Studio \& Visual Studio Code \\\\
% \\midrule
% Desarrollador \& Google/JetBrains \& Microsoft \\\\
% Soporte Nativo Android \& Completo (oficial) \& Mediante extensiones \\\\
% Emulador Integrado \& Sí \& No \\\\
% Gradle Integration \& Nativa \& Manual \\\\
% Jetpack Compose Preview \& Sí \& No \\\\
% \\bottomrule
% \\end{tabularx}
% \\caption{Comparación de IDEs para desarrollo Android.}\\label{tab:ide_comparison}
% \\small{Fuente: Android Developers \\cite{android_studio}}
% \\end{table}
% 
% Android Studio es el IDE oficial recomendado por Google para el desarrollo de aplicaciones Android. Basado en IntelliJ IDEA de JetBrains, proporciona un entorno optimizado con herramientas especializadas como el Layout Inspector, el Database Inspector para Room, y soporte nativo para Jetpack Compose \\cite{android_studio}.
% FIN SECCIÓN COMENTADA

\subsection{Paradigma de Interfaz Declarativa (Jetpack Compose)}
Tradicionalmente, las interfaces en Android se construían manipulando un árbol de vistas XML (enfoque imperativo). Actualmente, el uso de marcos de trabajo declarativos se ha consolidado en la industria, respaldado por estudios comparativos de rendimiento y mantenibilidad \cite{singh2025comparative, jain2023compose}.

El Cuadro \ref{tab:imperative_vs_declarative} resume las diferencias clave entre ambos paradigmas.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{4cm} p{4cm} p{4cm}}
\toprule
Característica & Enfoque Imperativo (XML) & Enfoque Declarativo (Compose) \\
\midrule
Manipulación de Vistas & Se manipula manualmente el árbol de vistas. & La UI se describe como una función del estado. \\
Gestión de Estado & El estado de la UI puede desincronizarse. & El estado es la única fuente de verdad. \\
Código & Código redundante (boilerplate). & Código conciso y legible. \\
\bottomrule
\end{tabularx}
\caption{Comparación entre UI Imperativa y Declarativa.}\label{tab:imperative_vs_declarative}
\small{Fuente: Basado en Singh et al. (2025) \cite{singh2025comparative}}
\end{table}

Jetpack Compose es el kit de herramientas moderno de Android para construir interfaces nativas. En este paradigma, la interfaz se describe como una función del estado actual de la aplicación.


Cuando el estado cambia, el marco de trabajo ejecuta un proceso de Recomposición. Este proceso actualiza selectivamente los componentes que dependen de los datos modificados. De esta forma, se reduce la complejidad de sincronización manual entre la vista y el modelo \cite{gupta2022modern}.

\subsection{Ciclo de Vida y Gestión de Estado (UDF)}
La gestión de estado es crítica en aplicaciones reactivas para garantizar la consistencia de los datos mostrados. Se utiliza el patrón Unidirectional Data Flow (UDF), donde el estado fluye en una sola dirección (de la lógica a la UI) y los eventos fluyen en sentido contrario (de la UI a la lógica).

Para implementar este patrón, se emplean flujos reactivos mediante StateFlow de la librería Kotlin Coroutines. StateFlow es un flujo observable que emite actualizaciones de estado inmutables a la interfaz de usuario. Por lo tanto, se garantiza que la interfaz refleje el último estado válido producido por la capa de lógica de negocio \cite{reactive_kotlin}.

\section{Arquitectura de Software}
\label{sec:arquitectura-software}

Para garantizar la mantenibilidad, facilidad de prueba y escalabilidad del software, se siguen principios de diseño robustos y patrones arquitectónicos estandarizados en la industria.

\subsection{Clean Architecture y Regla de Dependencia}
Clean Architecture, propuesta por Robert C. Martin \cite{martin2017-cleanarch}, estructura el código en capas concéntricas con responsabilidades claras, como se ilustra en la Figura \ref{fig:clean_architecture}.

\begin{figure}[H]
    \centering
    \includegraphics[width=0.75\textwidth]{images/CleanArchitecture.jpg}
    \caption{Diagrama conceptual de Clean Architecture}
    \label{fig:clean_architecture}
    \small{Fuente: Martin (2012) \cite{martin2012cleanarch_blog}}
\end{figure}


Las capas principales son:
\begin{itemize}
    \item Capa de Dominio (Domain): Contiene la lógica de negocio pura (Entidades y Casos de Uso). Es la capa más interna y no tiene dependencias de frameworks externos.
    \item Capa de Datos (Data): Implementa las interfaces definidas por el dominio y gestiona las fuentes de datos (base de datos local, red).
    \item Capa de Presentación (Presentation): Contiene la UI y los adaptadores de vista.
\end{itemize}
La Regla de Dependencia establece que las dependencias de código fuente solo pueden apuntar hacia adentro; el dominio no conoce detalles de la base de datos ni de la interfaz de usuario, lo que permite cambiar estas capas externas sin afectar la lógica central.

\subsection{Arquitectura Recomendada para Aplicaciones Android}
Google propone una arquitectura en capas que combina los principios de Clean Architecture con las particularidades del ecosistema Android. Esta arquitectura promueve la separación de responsabilidades y facilita las pruebas unitarias, mejorando la mantenibilidad del código a largo plazo.

El Cuadro \ref{tab:architecture_comparison} presenta una comparación entre los principales patrones arquitectónicos utilizados en el desarrollo de aplicaciones móviles.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & Clean Architecture & MVC (Model-View-Controller) \\
\midrule
Separación de Capas & Múltiples capas concéntricas & Tres capas principales \\
Dirección de Dependencias & Hacia el dominio (centro) & Bidireccional \\
Testabilidad & Alta (lógica aislada) & Moderada \\
Complejidad Inicial & Mayor & Menor \\
Mantenibilidad & Alta a largo plazo & Decrece con el crecimiento \\
\bottomrule
\end{tabularx}
\caption{Comparación de patrones arquitectónicos.}\label{tab:architecture_comparison}
\small{Fuente: Basado en Martin (2017) \cite{martin2017-cleanarch} y Gamma et al. (1995) \cite{gamma1995-patterns}}
\end{table}

Clean Architecture presenta ventajas en términos de mantenibilidad y testabilidad a largo plazo, aunque requiere una inversión inicial mayor en la estructuración del código. Esta arquitectura se compone de tres capas principales:

\begin{itemize}
    \item Capa de UI: Responsable de renderizar los datos y capturar las interacciones del usuario. Utiliza Jetpack Compose para construir interfaces declarativas. Los componentes clave incluyen Composables (funciones que describen la UI) y ViewModels (gestionan el estado y sobreviven a cambios de configuración).
    \item Capa de Dominio: Encapsula la lógica de negocio reutilizable. Los Casos de Uso representan acciones específicas del usuario y orquestan la interacción entre repositorios.
    \item Capa de Datos: Fuente única de verdad (Single Source of Truth). Expone los datos a través de repositorios que combinan múltiples fuentes (bases de datos locales, APIs remotas) \cite{android_data_layer_guide}.
\end{itemize}

\subsection{Inyección de Dependencias (Hilt)}
La Inyección de Dependencias (DI) es un patrón donde los objetos reciben sus dependencias desde fuera, promoviendo la Inversión de Control (IoC) \cite{dependency_injection_principles}. Hilt es la solución estándar de Google para DI en Android, reduciendo el código repetitivo y facilitando la modularidad.

\section{Fundamentos Algorítmicos y de Optimización}
\label{sec:algoritmia}

El problema de generación de horarios académicos es conocido formalmente en la literatura como University Course Timetabling Problem (UCTP). Este problema se aborda mediante diversas teorías y algoritmos de optimización.

\subsection{Teoría de Grafos en la Planificación}
La Teoría de Grafos proporciona un marco matemático robusto para modelar conflictos en problemas de planificación. El problema de horarios se puede representar como un problema de coloración de grafos (Graph Coloring Problem - GCP) \cite{lewis2015guide}.

En este modelo:
\begin{itemize}
    \item Vértices (Nodos): Representan los eventos o clases a programar.
    \item Aristas (Enlaces): Conectan dos vértices si existe un conflicto entre ellos (por ejemplo, mismo profesor, mismo grupo de estudiantes o misma aula).
    \item Colores: Representan los periodos de tiempo disponibles.
\end{itemize}

El objetivo es asignar un color a cada vértice de tal manera que ningunos dos vértices adyacentes compartan el mismo color, utilizando el mínimo número de colores posible (número cromático).

Algoritmos clásicos como Welsh-Powell o DSATUR (Degree of Saturation) son eficientes para encontrar soluciones en grafos de conflictos \cite{burke2004-state}. No obstante, las restricciones inherentes a la planificación académica (preferencias de docentes, minimización de huecos, restricciones de capacidad) limitan la aplicabilidad directa del modelo de coloración pura.

Por esta razón, aunque la teoría de grafos fundamenta el entendimiento de los conflictos, un enfoque alternativo y flexible es el uso de CSP y Backtracking.

\subsection{Problema de Satisfacción de Restricciones (CSP)}
El UCTP se modela matemáticamente como un Problema de Satisfacción de Restricciones (CSP), definido por Russell y Norvig \cite{russell2021ai} como una terna $(X, D, C)$:
\begin{itemize}
    \item $X$: Un conjunto de variables (las asignaturas a inscribir).
    \item $D$: Un conjunto de dominios (los grupos disponibles para cada asignatura).
    \item $C$: Un conjunto de restricciones que especifican combinaciones permitidas de valores.
\end{itemize}

Las restricciones se clasifican en Duras (Hard), que deben cumplirse obligatoriamente (por ejemplo, no solapamiento de horarios), y Blandas (Soft), que expresan preferencias deseables pero no obligatorias (por ejemplo, minimizar huecos libres).

\subsection{Algoritmos de Búsqueda y Backtracking}
Para resolver problemas de tipo CSP, comúnmente se utiliza el algoritmo de Backtracking (Vuelta Atrás). Este método realiza una búsqueda sistemática en el espacio de soluciones, explorando las posibles asignaciones de manera incremental y descartando ramas que violan las restricciones establecidas:

\begin{enumerate}
    \item Generación de Candidatos: El algoritmo construye soluciones de manera incremental, asignatura por asignatura.
    \item Verificación de Restricciones: En cada paso, verifica si la asignación actual viola alguna restricción dura.
    \item Retroceso (Backtrack): Si se encuentra un conflicto insalvable, el algoritmo retrocede al paso anterior y prueba con el siguiente grupo disponible, descartando la rama actual del árbol de búsqueda \cite{cormen_algorithms}.
\end{enumerate}

Este enfoque permite identificar el conjunto completo de combinaciones válidas posibles. Dicha característica resulta fundamental para presentar al estudiante todas las opciones disponibles.

\subsection{Optimización Combinatoria y Selección}
Dado que el número de combinaciones válidas puede ser muy grande, se aplica un proceso de Optimización Combinatoria para seleccionar las mejores opciones. Los algoritmos de optimización no solo buscan una solución factible, sino que buscan las ``mejores $k$'' soluciones según criterios de calidad.

Se suele utilizar un sistema de ranking donde cada horario generado recibe una puntuación basada en el cumplimiento de restricciones blandas. Posteriormente, se ordenan las soluciones de mayor a menor puntaje y se presentan al usuario las opciones destacadas, facilitando el proceso de selección.

\subsection{Análisis de Decisión Multi-criterio (MCDA)}
Para evaluar la calidad de un horario, se emplea el Análisis de Decisión Multi-criterio (MCDA). Este enfoque permite considerar múltiples factores simultáneamente, asignando pesos específicos a cada criterio \cite{triantaphyllou2000multi}.

En la práctica, esto se puede implementar a través del Patrón de Diseño \textit{Strategy}. Este patrón permite cambiar dinámicamente la estrategia de evaluación (por ejemplo, ``Priorizar mañanas libres'', ``Minimizar huecos'', ``Evitar cruces de materias''), calculando el puntaje total como una suma ponderada de estos criterios. Esto otorga flexibilidad para adaptarse a las preferencias cambiantes.

\section{Inteligencia Artificial en Dispositivos Móviles (Edge AI)}
\label{sec:edge-ai}

La Inteligencia Artificial en el Borde (Edge AI) permite integrar capacidades de inteligencia artificial generativa ejecutándose directamente en el dispositivo móvil. Este paradigma elimina la dependencia de servidores externos, garantizando la privacidad de los datos del usuario y reduciendo la latencia de respuesta. Además, posibilita el funcionamiento de características inteligentes en escenarios sin conectividad a internet.

\subsection{Frameworks de Inferencia en Dispositivos}
Existen diversas librerías para implementar inferencia de modelos en dispositivos móviles. El Cuadro \ref{tab:edge_frameworks} presenta una comparación de las principales opciones disponibles.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & llama.cpp & TensorFlow Lite \\
\midrule
Tipo de Modelo & LLMs (texto) & Modelos generales \\
Lenguaje Base & C++ (JNI) & C++ / Java \\
Cuantización & GGUF (4-bit) & INT8/FP16 \\
Tamaño de Modelos & 1-7GB típico & KB a MB \\
Caso de Uso Principal & IA Generativa & Visión/Clasificación \\
\bottomrule
\end{tabularx}
\caption{Comparación de frameworks de Edge AI.}\label{tab:edge_frameworks}
\small{Fuente: Basado en Gholami et al. \cite{quantization_papers} y Gerganov \cite{llama_cpp}}
\end{table}

La selección del framework depende del tipo de modelo y los requisitos de la aplicación. Para modelos de lenguaje grande (LLMs), librerías como llama.cpp ofrecen optimizaciones específicas mediante cuantización agresiva en formato GGUF, permitiendo ejecutar la inferencia de manera eficiente en la CPU de dispositivos Android a través de JNI (Java Native Interface) \cite{llama_cpp}.

\subsection{Modelos de Lenguaje (LLMs) y Cuantización}
Para viabilizar la ejecución de Large Language Models (LLMs) en dispositivos con recursos limitados, se utiliza la técnica de Cuantización. Este proceso reduce la precisión de los pesos del modelo (de punto flotante de 16 bits a enteros de 4 bits), disminuyendo el tamaño y consumo de memoria con pérdida mínima de precisión \cite{quantization_papers}. El formato GGUF está optimizado para inferencia rápida en CPU.

\subsection{Fine-Tuning y Arquitecturas Líquidas}
El Fine-Tuning es el proceso de adaptar un modelo pre-entrenado a un dominio o tarea específica. El Cuadro \ref{tab:finetuning_comparison} presenta las principales técnicas disponibles.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & Full Fine-Tuning & LoRA/QLoRA \\
\midrule
Parámetros Entrenados & Todos & Subconjunto reducido \\
Requisitos de GPU & Muy altos (16-80GB) & Moderados (4-16GB) \\
Tiempo de Entrenamiento & Extenso & Reducido \\
Riesgo de Overfitting & Mayor & Menor \\
Flexibilidad & Máxima & Limitada \\
\bottomrule
\end{tabularx}
\caption{Comparación de técnicas de Fine-Tuning.}\label{tab:finetuning_comparison}
\small{Fuente: Basado en Hu et al. (2021) \cite{lora_paper} y Hugging Face \cite{llm_finetuning}}
\end{table}

Las arquitecturas Liquid Foundation Models (LFM) representan un enfoque híbrido que combina diferentes tipos de capas neuronales, optimizando el balance entre rendimiento y eficiencia computacional \cite{liquidai_lfm}.

\subsection{Parámetros de Muestreo en Generación de Texto}
La generación de texto en LLMs utiliza diversos parámetros de muestreo (sampling) que controlan la aleatoriedad y calidad de las respuestas:

\begin{itemize}
    \item Temperature: Controla la aleatoriedad. Valores bajos (0.1-0.5) producen respuestas más determinísticas, mientras valores altos (0.8-1.5) generan mayor variedad.
    \item Top-P (Nucleus Sampling): Limita la selección a tokens cuya probabilidad acumulada no exceda el umbral P.
    \item Top-K: Restringe la selección a los K tokens más probables.
    \item Repeat Penalty: Penaliza la repetición de tokens para evitar bucles en la generación.
\end{itemize}

La combinación adecuada de estos parámetros permite optimizar la generación según el caso de uso específico \cite{holtzman2020curious}.

\section{Adquisición y Procesamiento de Datos}
\label{sec:adquisicion-datos}

Las aplicaciones móviles frecuentemente requieren obtener y procesar datos de fuentes externas heterogéneas, como páginas web y documentos PDF.

\subsection{Web Scraping}
El Web Scraping es la técnica de extracción automática de datos desde páginas web. El Cuadro \ref{tab:scraping_comparison} compara las principales bibliotecas disponibles.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & Jsoup & Selenium \\
\midrule
Tipo & Parser HTML estático & Automatización de navegador \\
JavaScript & No ejecuta & Ejecuta completamente \\
Rendimiento & Rápido y ligero & Lento (carga navegador) \\
Complejidad & Baja & Alta \\
Uso en Android & Nativo & No recomendado \\
\bottomrule
\end{tabularx}
\caption{Comparación de tecnologías de Web Scraping.}\label{tab:scraping_comparison}
\small{Fuente: Basado en Mitchell (2018) \cite{mitchell2018scraping} y documentación oficial \cite{jsoup_docs}}
\end{table}

Jsoup es una biblioteca Java que proporciona una API similar a jQuery para navegar y manipular el DOM, permitiendo extraer datos mediante selectores CSS \cite{jsoup_docs}.

\subsection{Extracción de Texto desde Documentos PDF}
El procesamiento de documentos PDF requiere bibliotecas especializadas capaces de interpretar el formato binario del documento. PDFBox es una biblioteca de código abierto de Apache que permite leer, crear y manipular documentos PDF, incluyendo la extracción de texto estructurado \cite{pdfbox_docs}.

El proceso típico incluye: descarga del archivo, apertura del documento, iteración sobre páginas, extracción de texto mediante PDFTextStripper, y finalmente parsing del contenido extraído con expresiones regulares para identificar patrones específicos.

\subsection{Patrones de Resiliencia en Comunicación de Red}
Las aplicaciones que dependen de servicios externos deben implementar mecanismos de tolerancia a fallos. El Cuadro \ref{tab:resilience_comparison} compara los principales patrones.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & Circuit Breaker & Retry Simple \\
\midrule
Mecanismo & Bloquea tras N fallos & Reintenta inmediatamente \\
Protección del Servidor & Sí (reduce carga) & No (puede saturar) \\
Recuperación & Automática con half-open & Manual \\
Complejidad & Moderada & Baja \\
Combinación Recomendada & Con Exponential Backoff & Solo para errores transitorios \\
\bottomrule
\end{tabularx}
\caption{Comparación de patrones de resiliencia.}\label{tab:resilience_comparison}
\small{Fuente: Basado en Nygard (2018) \cite{nygard2018release}}
\end{table}

El patrón Exponential Backoff complementa estas estrategias incrementando progresivamente los intervalos de reintento, reduciendo la carga sobre servicios saturados.

\section{Gestión y Persistencia de Datos}
\label{sec:persistencia}

La filosofía Offline-First garantiza funcionalidad sin conexión a internet \cite{guda2025offline}. Las aplicaciones móviles requieren soluciones de persistencia optimizadas. El Cuadro \ref{tab:db_comparison} compara las principales tecnologías disponibles.

\begin{table}[H]
\small
\centering
\begin{tabularx}{\linewidth}{p{3.5cm} p{4.5cm} p{4.5cm}}
\toprule
Característica & Room (SQLite) & Realm \\
\midrule
Tipo de Base de Datos & Relacional (SQL) & Orientada a Objetos \\
Lenguaje de Consulta & SQL estándar & Consultas propias \\
Verificación en Compilación & Sí & No \\
Soporte Oficial Google & Sí (Jetpack) & No \\
Curva de Aprendizaje & Moderada & Baja \\
\bottomrule
\end{tabularx}
\caption{Comparación de tecnologías de base de datos para Android.}\label{tab:db_comparison}
\small{Fuente: Basado en Android Developers \cite{room_db} y Silberschatz et al. \cite{database_concepts}}
\end{table}

SQLite es un motor de base de datos relacional ligero embebido en Android. Room proporciona una capa de abstracción orientada a objetos sobre SQLite, permitiendo definir Entidades y DAOs con verificación de consultas SQL en tiempo de compilación \cite{database_concepts}. Esta combinación ofrece ventajas en términos de integración con el ecosistema Android y verificación de errores en tiempo de compilación \cite{room_db}.

\subsection{Estrategias de Sincronización y Offline-First}
El patrón Offline-First trata la conectividad como un recurso opcional. La base de datos local actúa como fuente primaria de datos (Single Source of Truth), mientras que las operaciones de red se ejecutan en segundo plano mediante WorkManager \cite{couchbase_offlinefirst}. Las librerías de persistencia modernas notifican automáticamente a la UI mediante flujos reactivos cuando la sincronización completa.

\section{Notificaciones y Widgets en Android}
\label{sec:notificaciones-widgets}

Android proporciona mecanismos para interactuar con el usuario fuera de la aplicación: Notificaciones y App Widgets. Las notificaciones requieren asignarse a un Canal (Notification Channel) desde Android 8.0 \cite{android_notifications}. Existen dos tipos: Locales (programadas con WorkManager) y Remotas (vía Firebase Cloud Messaging).

\subsection{App Widgets}
Los App Widgets son vistas miniatura incrustables en la pantalla de inicio \cite{android_widgets}. Su arquitectura se basa en AppWidgetProvider (BroadcastReceiver que maneja el ciclo de vida) y RemoteViews (describe vistas ejecutables en otro proceso) \cite{android_remoteviews}.
